using System;

namespace Calc_nogui
{
    class Calc_nogui
    {
        private bool modeCalc;
        private Read read = default!;

        public void run(string[] args)
        {
            string argument;
        }
        public static void Main(string[] args)
        {
            var calc_nogui = new Calc_nogui();
            calc_nogui.run(args);
        }
    }
}